# Model artifact policy

Model binaries remain local. Future model records require hashes, software versions, training manifests, configuration hashes, seeds, and limitations. Week 1 creates no model.
