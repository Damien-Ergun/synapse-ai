# Reports and evidence policy

Committed reports contain sanitised summaries and evidence identifiers. `reports/logs/` and `reports/generated/` remain local and ignored. Retained logs require SHA-256 entries in the committed evidence manifest without private content or machine-specific absolute paths.
