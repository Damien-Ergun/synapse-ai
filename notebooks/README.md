# Notebook policy

Notebooks are exploratory and explanatory. Reusable logic belongs in `src/`. Use repository-relative paths, record configuration and manifest identifiers, prevent reserved-split leakage, and clear sensitive or bulky outputs before commit.
